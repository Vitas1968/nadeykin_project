[build-system]
requires = ["setuptools>=70"]
build-backend = "setuptools.build_meta"

[project]
name = "tender-analyzer"
version = "0.1.0"
description = "Tender documents analyzer"
requires-python = ">=3.13"

[tool.setuptools.packages.find]
where = ["src"]
